{
    // Telegram Bot API token
    "tokens": [],

    // Name of the file for state persistence
    // Leave empty to disable persistence
    "db": "bin/db",

    // Minimum log level
    "log_level": "info"
}